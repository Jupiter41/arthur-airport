# Archive

Historical, superseded material kept for provenance only. **Nothing here is
authoritative or maintained.** Do not link to it from active docs.

- `docs-old/` — early planning docs, per-sprint notes, and bug reports from the
  bring-up phase (formerly `docs/old/`, including a stale nested
  `lessons-learned/`). Superseded by the current `docs/` tree and the active,
  referenced `docs/lessons-learned/`.

For current specifications see `docs/`; for the current improvement roadmap see
`ROADMAP_REAL_LDT.md` at the repo root.
